{
  "name": "@saeon/mermaid",
  "version": "0.0.1",
  "description": "Mermaid diagrams",
  "type": "module",
  "scripts": {
    "build": "npm run deployment-view && npm run user-view",
    "deployment-view": "docker run -it -v /home/zach/code/catalogue/diagrams:/data minlag/mermaid-cli:latest -C /data/style.css -c /data/config.json -i /data/src/deployment-view.mmd -o /data/dist/deployment-view.png",
    "user-view": "docker run -it -v /home/zach/code/catalogue/diagrams:/data minlag/mermaid-cli:latest -C /data/style.css -c /data/config.json -i /data/src/user-view.mmd -o /data/dist/user-view.png"
  },
  "author": "zach@saeon.ac.za",
  "license": "MIT",
  "dependencies": {
    "mermaid.cli": "^0.5.1"
  }
}
