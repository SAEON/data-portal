scalar Date
scalar JSON

type Map {
  id: ID!
  name: String
}

type Query {
  search(dsl: JSON!): JSON
  maps: [Map]!
}

type Subscription {
  onFilterChange: JSON
}

type Mutation {
  upsertMap: Map!
}
