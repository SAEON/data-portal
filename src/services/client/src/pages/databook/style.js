import { makeStyles } from '@material-ui/core/styles'

export default makeStyles(() => ({
  layout: {
    position: 'absolute',
    top: 0,
    bottom: 0,
    left: 0,
    right: 0,
  },
  pushDown: {
    marginTop: 48,
  },
}))
