export default record => {
  // const { institution = '', id } = record
  // if (institution.toUpperCase() === 'CHIEF DIRECTORATE: OCEANS AND COASTAL RESEARCH') {
  //   console.info('Filtering out MIMS record', id)
  //   return false
  // }

  return true
}
